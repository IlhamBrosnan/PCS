import 'package:flutter/material.dart';

class Send extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text('Halaman IndoSend'),
      ),
    );
  }
}